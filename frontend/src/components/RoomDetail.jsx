import React from 'react';

const RoomDetail = ({ date, roomNumber }) => {
  // Implement the component logic and UI to display relevant information
  return (
    <div>
      <h2>Room Detail</h2>
      <p>Date: {date}</p>
      <p>Room Number: {roomNumber}</p>
      {/* Add more relevant information */}
    </div>
  );
};

export default RoomDetail;
